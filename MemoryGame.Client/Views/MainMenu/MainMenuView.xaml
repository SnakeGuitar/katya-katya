<UserControl x:Class="MemoryGame.Client.Views.MainMenu.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="800" d:DesignHeight="450">

    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock FontSize="20" HorizontalAlignment="Center" Margin="0,0,0,30">
                <Run Text="Welcome, " />
                <Run Text="{Binding Username, Mode=OneWay}" FontWeight="Bold" />
            </TextBlock>

            <Button Content="Singleplayer"
                    Height="40" Margin="0,0,0,10" />

            <Button Content="Multiplayer"
                    Height="40" Margin="0,0,0,10" />

            <Button Content="Profile"
                    Height="40" Margin="0,0,0,10" />

            <Button Content="Friends"
                    Height="40" Margin="0,0,0,10" />

            <Button Content="Settings"
                    Height="40" Margin="0,0,0,20" />

            <Button Content="Logout"
                    Command="{Binding LogoutCommand}"
                    Height="35" Style="{StaticResource LinkButton}" />
        </StackPanel>
    </Grid>
</UserControl>
